"""
tools/__init__.py
-----------------
Placeholder package for Week 2/3 CrewAI Tools.

Planned tools (not implemented in Week 1):
    * SentimentTool       — fine-grained sentiment scoring
    * ThemeExtractorTool  — keyword / topic extraction over reviews
    * SummarizerTool      — abstractive summary of a review batch
    * DatasetSearchTool   — query the local reviews.csv for similar cases

Kept empty on purpose so the import path ``from tools import X`` is
already valid when we add them.
"""
